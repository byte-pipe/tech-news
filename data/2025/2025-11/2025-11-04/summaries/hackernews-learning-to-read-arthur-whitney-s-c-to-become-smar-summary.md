---
title: "Learning to read Arthur Whitney's C to become Smart - Needleful.net"
url: https://needleful.net/blog/2024/01/arthur_whitney.html
date: 2025-11-04
site: hackernews
model: llama3.2:1b
summarized_at: 2025-11-04T11:22:37.364490
screenshot: hackernews-learning-to-read-arthur-whitney-s-c-to-become-smar.png
---

# Learning to read Arthur Whitney's C to become Smart - Needleful.net

# Learning to Read Arthur Whitney's C to Become Smart

## Introduction to Kdb and Its Significance
Arthur Whitney is a renowned computer scientist who has led the design of several notable programming languages. One such language is kdb, a high-performance database built on top of his own languages APL and Qwik, used heavily in fintech applications.

## Key Features of Early kdb Versions
Whitney's early versions of kdb demonstrated significant advancements in interpretive programming, particularly for handling large datasets, utilizing unique character encoding, rank, domain, and nyi data structures. The publicly accessible interpreter for the C version demonstrates a straightforward implementation of these concepts using minimal code.

## Understanding the Structure and Syntax of kdb
The provided C code snippet illustrates various key components:
- **typedefs**: define constants for character strings (`s`, `c`).
- **macro definitions** (e.g., `_a$, _h`) for string manipulation, arithmetic operations, indexing, and comparisons.
- **type casting functions**: used to manipulate variables' data types.

## Writing C in kdb
One notable aspect of writing C in kdb is the use of a simple memory management system, allowing direct usage of various character strings. The code snippets below demonstrate its execution on a simplified kdb structure using `n` as a variable representing rows and columns:

### Simple Function Example

The provided function `_a(a=5)` showcases a straightforward example that uses arithmetic operations to calculate product.

## Conclusion
Arthur Whitney's work in kdb continues to shape the fintech industry, especially when it comes to handling large-scale datasets. The C version mentioned in this article serves as an entry point for those interested in the programming language itself, providing insight into its unique features and how they were implemented in a simplified context.

# Keyword List:

* arith
* at
* cat
* cnt
* cut
* drp
* echo
* error
* f
* func
* g
* hex
* if
* ind
* i
* insert
* io
* k
* ltn
* map
* memo
* not
* num
* open
* of
* on
* out
* ord
* padr
* q
* read
* rank
* repeat
* return
* rsort
* seteq
* sizeof
* sort
* str
* sub
# Keyword List:

* `a` (integer)
* `c` (character string)
* `e` (number of bytes for `s`)
* `f` (function object)
* `i` (integer index)
* `n` (number of elements in a vector or array)
* `o` (output stream)
* `r` (input stream)
# Keyword List:

* `!` (logical not)
* `==` (equality comparison)
* `&` (bitwise AND operation)
* `.~` (numeric bitwise NOT operator)
* `0` (zero number)
* `<` (less than)
* `=` (equal sign equals)
* `?=` (double assignment)
* `>` (greater than)
* `!=` (inverted logical not equals)
* `#` (logical AND operation)
* `|>` (absolute value operation)
* `~` (numeric NOT operator)
* `&!` (logical AND bitwise XOR operation)
* `.w<` `._b` `..s`
