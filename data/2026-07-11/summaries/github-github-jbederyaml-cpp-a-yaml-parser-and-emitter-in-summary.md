---
title: GitHub - jbeder/yaml-cpp: A YAML parser and emitter in C++ · GitHub
url: https://github.com/jbeder/yaml-cpp
date: 
site: github
model: gpt-oss:120b-cloud
summarized_at: 2026-07-11T01:39:42.473797
---

# GitHub - jbeder/yaml-cpp: A YAML parser and emitter in C++ · GitHub

# yaml-cpp Repository Summary

## Overview
- yaml-cpp is a YAML parser and emitter written in C++ that follows the YAML 1.2 specification.  
- Distributed under the MIT license.  

## Usage
- Main reference: Tutorial and How to Emit YAML.  
- For the legacy API (up to version 0.5.0), see “How To Parse A Document”.  

## Building
- Build system: CMake (cross‑platform).  
- Basic steps:  
  1. Create a build directory inside the source tree.  
  2. Run CMake, optionally specifying a generator and `-DYAML_BUILD_SHARED_LIBS=ON` to build a shared library.  
  3. Build with the toolchain generated by CMake.  
- Default builds a static library; use the `-DYAML_BUILD_SHARED_LIBS` flag for a shared one.  
- Debug builds can enable `_GLIBCXX_DEBUG`; GoogleTest must be built with the same flag.  
- Additional configuration options are available in `CMakeLists.txt`.  

## CMake Integration Example
```cmake
include(FetchContent)

FetchContent_Declare(
  yaml-cpp
  GIT_REPOSITORY https://github.com/jbeder/yaml-cpp.git
  GIT_TAG <tag_name>   # tag, commit hash, or branch
)

FetchContent_MakeAvailable(yaml-cpp)

target_link_libraries(
  YOUR_LIBRARY
  PUBLIC
  yaml-cpp::yaml-cpp
)
```

## Releases
- Latest release: **0.9.0** (4 Feb 2026).  
- Legacy API available in the 0.3.x series; bug‑fix support for the old API ends in 2026.  
- Versions 0.5.x and newer implement the new API.  

## Documentation
- Auto‑generated API reference hosted on CodeDocs.  
- Additional docs: README, CONTRIBUTING, SECURITY, install instructions.  

## Third‑Party Integrations (unofficial)
- Qt wrapper.  
- Unreal Engine wrapper.  

## Repository Statistics
- Stars: 6.1 k  
- Forks: 2.3 k  
- Watchers: 171  
- Primary language distribution: C++ 91.3 %, Python 5.4 %, C 1.1 %, CMake 1.0 %, others <1 %.  

## File Structure Highlights
- `include/yaml-cpp` – public headers.  
- `src` – implementation source files.  
- `test` – unit tests.  
- `docs` – documentation files.  
- Build scripts: `CMakeLists.txt`, `BUILD.bazel`, `MODULE.bazel`, etc.