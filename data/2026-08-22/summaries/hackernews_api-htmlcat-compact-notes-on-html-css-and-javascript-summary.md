---
title: HTMLcat — Compact notes on HTML, CSS, and JavaScript
url: https://htmlcat.net/
date: 2026-08-21
site: hackernews_api
model: gpt-oss:120b-cloud
summarized_at: 2026-08-22T06:00:53.378783
---

# HTMLcat — Compact notes on HTML, CSS, and JavaScript

# HTMLcat — Compact notes on HTML, CSS, and JavaScript

## Overview
- A curated collection of small, native web tricks that are useful to remember.  
- Each “post‑it” pairs a platform feature with a concise example and highlights any important caveats.  
- Some notes cover limited or experimental features; they include a support label, recommend fallbacks, and advise testing with real browsers and assistive technologies.

## Structure
- The site offers a searchable list of post‑its.  
- Content is organized by technology (HTML, CSS, JavaScript) and can be filtered by “All” or specific categories.  
- A total of 42 post‑its are currently available.

## Topics Covered
### JavaScript
- Device type detection  
- classList.toggle()  
- Localized prices with `Intl.NumberFormat`  
- Scroll‑driven animations (View Transitions API)  
- Scroll snap events  
- Scroll margin (combined with CSS)  
- Smooth scrolling (combined with CSS)  

### CSS
- Hide scrollbars  
- Empty tags styling  
- Centering techniques  
- `clamp()` function  
- Smooth scrolling (combined with JS)  
- `:has()` relational selector  
- Styling list markers with `::marker`  
- `prefers‑color‑scheme` media query  
- Container size queries & container style queries  
- `text‑wrap` for better line breaking  
- Description list alignment (also HTML)  
- Native form accent colors (`accent-color`)  
- CSS Custom Highlight API (also JS)  
- Detect hover across input types  
- Transparent colors with `color‑mix()`  
- `light‑dark()` and `contrast‑color()` functions  
- Scoped CSS with `@scope`  
- `:where()` selector  
- Scroll‑driven animations  
- CSS carousels (also HTML)  
- Corner‑shape utilities  
- Custom CSS functions  
- CSS Grid Lanes  
- Popover API (also HTML)  

### HTML
- Telephone and email links  
- `:target` pseudo‑class (also CSS)  
- `translate` global attribute  
- `inert` attribute to disable a subtree  
- Dialog element (also JS)  
- Description list alignment (also CSS)  
- Exclusive accordions with `details name`  
- Controlling dialogs with button commands  
- Searchable collapsed content using `hidden="until‑found"`  

### Bash
- Sort directories by size  

## Usage Tips
- Verify browser support for experimental features before relying on them.  
- Provide fallbacks where necessary to maintain functionality across environments.  
- Use the built‑in search to quickly locate relevant post‑its by title or content.