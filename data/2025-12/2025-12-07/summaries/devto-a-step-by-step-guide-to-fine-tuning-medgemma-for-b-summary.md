---
title: A step-by-step guide to fine-tuning MedGemma for breast tumor classification - DEV Community
url: https://dev.to/googleai/a-step-by-step-guide-to-fine-tuning-medgemma-for-breast-tumor-classification-35af
date: 2025-12-02
site: devto
model: llama3.2:1b
summarized_at: 2025-12-07T11:19:30.002271
screenshot: devto-a-step-by-step-guide-to-fine-tuning-medgemma-for-b.png
---

# A step-by-step guide to fine-tuning MedGemma for breast tumor classification - DEV Community

**Fine-tuning MedGemma for Breast Tumor Classification**
=====================================================

### Overview
This guide provides a step-by-step walkthrough of fine-tuning the MedGemma model for breast cancer histopathology image classification. We'll use the `Finetune Notebook` to perfect our model in a prototyping phase and then adapt it for production-ready deployment using Cloud Run jobs.

**Disclaimer**
-------------

Fine-tunning MedGemma is not a substitute for professional medical advice, diagnosis, or treatment. It's essential to validate and correct any models generated by MedGemma through independent verification, clinical correlation, and further research.

### Setup
----------------

Before we begin, let's set up our environment:

* Install the necessary libraries: PyTorch, Keras, TensorFlow
* Import the required libraries
* Set up a basic Python notebook with Jupyter or IDE

**Key Insights**

* We'll use the entire MedGemma model for classification, which requires significant computational resources.
* Quantization is an effective method to reduce compute costs.

### Fine-tuning Model

#### Step 1: Prepare Data

* Load and preprocess breast tissue histopathology images from a reliable dataset
* Reshape and normalize the data to suit PyTorch/CUDA architecture

```python
import torch
from torchvision import datasets, transforms
from sklearn.model_selection import train_test_split
import numpy as np

# Load and preprocess breast tissue histopathology images
train_data = datasets.ImageList("path/to/train/dataset")
test_data =datasets.ImageList("path/to/test/dataset")

transform = transforms.Compose([
    transforms.Resize((224, 224)),  # resize to 224x224
    transforms.ToTensor()
])

# Get a batch of data for training and validation
train_loader = torch.utils.data.DataLoader(
    train_data, batch_size=32, shuffle=True
)
```

#### Step 2: Load and Prepare MedGemma Model

```python
import MedGemma

# Load the pre-trained MedGemma model
model = MedGEmma.MedGemma(vocabulary_path="path/to/vocabulary/file")

# Freeze all layers except for the last one (final output layer)
for param in model.parameters():
    param.requires_grad = False
```

#### Step 3: Fine-tune Model

```python
# Create a tensorboard object to visualize progress and metrics
import torch.utils.data as data
from tensorboard.xglue import TensorBoard

# Set up the tensorboard object
tb = TensorBoard("fine-tuning")

# Train the model with early stopping and evaluation on validation set
def train(model, loader, optimizer, device):
    # Train model for 100 epochs with early stopping at epoch 10
    for epoch in range(10):
        model.train()
        running_loss = 0.0
       running_corrects = 0.0

        for data, targets in tqdm(zip(loader["data"], loader["labels"])):
            optimizer.zero_grad()

                        data.to(device)
                    # forward pass: map input to output tensors (classification heads) and loss calculation
                y_output = model(data["image"].to(device))
                loss = MedGemma.MedGemma.Loss(y_output, targets)

                                running_loss += MedGemma.MedGemma.RATE * loss.backward()
                            running_corrects += int(np.round(0.5 + 0.1*targets)))

        loss.backward()  # calculate gradient magnitude for each parameter group
                              # store the gradients of input images and outputs in a tensorboard object

        optimizer.step()

    print(f"Epoch {epoch+1}, Loss = {running_loss / len(loader) * MedGemma.MedGemma.LRate:.4f}")
    print(f"Accuracy on validation set = {(running_corrects/len(loader)):.2f}")

tb.add_scalar("Loss", running_loss / len(loader), epoch=1)
tb.add_scalar("Acc", (running_corrects/len(loader)), epoch=1)

# Call the train function to start training the model
```

#### Step 4: Deploy Production-ready Model using Cloud Run

* Deploy a production-ready model on Google Cloud Run using your preferred framework.

### Next Steps
-----------------

For more information on fine-tuning MedGemma or deploying production-ready models, please check out our ongoing tutorials and research articles:

https://devcommunity.google.com/questions/16038/easy-fine-tuning-medgemma

Please note that the code snippets above are basic examples to demonstrate the steps. You may need to modify them depending on the specific requirements of your project.

Best of luck with fine-tuning MedGemma for breast cancer histopathology image classification!
