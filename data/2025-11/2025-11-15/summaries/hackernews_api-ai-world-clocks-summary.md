---
title: AI World Clocks
url: https://clocks.brianmoore.com/
date: 2025-11-14
site: hackernews_api
model: llama3.2:1b
summarized_at: 2025-11-15T11:14:46.639870
screenshot: hackernews_api-ai-world-clocks.png
---

# AI World Clocks

## AI World Clocks
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI World Clocks</title>
    <link rel="stylesheet" href="style.css"> <!-- Load CSS file -->
</head>
<body>
<script src="https://unpkg.com/@fullstackmylife/clock-js@1.0.19/dist/counterclock.js"></script>
<style media query* only screen and (max-width: 600px) {
    /* Add responsive design */
}
body {
    background-color: #fff;
}
#clock-container {
    position: relative;
    width: 400px;
    height: 200px; /* Adjust according to your needs */
    border: 1px solid black;
}
.time-container {
    font-size: 30px;
    vertical-align: middle;
}
.second-hand {
    stroke-width: 2;
    stroke-linecap: round;
    stroke-dasharray: ${numbers} ${numbers};
    fill-opacity: 0.5;
}
```
```css
#clock-container {
    position: relative;
}

#clock-container #time-container {
    font-family: digital-2, sans-serif;
    text-align: center;
}
```
```javascript
let clock = null;

function generateClock(num) {
    if (clock === null) return; // Ensure only one clock is displayed

    const angle = num * Math.PI / 180; // Convert numbers to degrees

    // Generate HTML/CSS code for the current minute
    const htmlTemplate = `\
        <div id="clock-container">
            <div class="time-container">${num % 60 < 10 ? '0' + num % 60 : num % 60}</div>
            <span class="second-hand">, </span>\n`;
    document.getElementById('clock-container').innerHTML = htmlTemplate;

    // Create the clock (9 AI models)
    const clockHtml = `
        ${Object.keys(clock.models).map(model => `<img src="${clock.generateModelUrl(model)}" alt="${model}">`).join('\n')}
        <h1>Generated by: ${clock.createId()}</h1>\n`;

    document.getElementById('clock-container').innerHTML += clockHtml;
}

function animateSecondHand(num) {
    let angle = num * Math.PI / 180; // Convert numbers to degrees
    let speed = `calc(${numbers} + (2 * ${Math.PI / 60}))%`;

    const secondHand = document.getElementById('second-hand');
    secondHand.style.strokeDasharray = `${numbers '${numbers}'}; ${angle='${speed}}}';

    if (numbers !== '' && counterclock !== 'done') { // Display numbers only once
        counterclock(num);
    }

    setTimeout(() => {
        animateSecondHand(--speed);
    }, 500 + parseInt(speed));

    const secondRadius = 15;
    let distance = Math.sqrt(Math.PI * number / 360 ** 2) - 5;

    console.log(`Current angle: ${angle}, Time left over: ${distance}`);
}

let currentNumber = 14; // Change this to start the clock
let counterclock = null; // Track multiple clocks

generateClock(currentNumber);

// Initialize an AI model and display it whenever the counterclock finishes
function createId() {
    let models = ['Alpha', 'Beta', 'Gamma', 'Delta', 'Epsilon'];
    return `${Math.random()}.${models[Math.floor(Math.random()) % models.length]}`;
}

let modelIndex = 1; // Starting AI Model Index

// Display the first clock and start generating it
generateClock(currentNumber);
```
This responsive HTML/CSS code combines generated clocks from 9 different AI model images to create a single analog clock displaying numbers.
